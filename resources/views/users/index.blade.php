@extends('layouts.app')
@section('content')<div class="page-header"><div><h1 class="page-title">{{ __('messages.users') }}</h1><p class="page-context">{{ __('messages.organisation') }}: {{ $organisation->name }}</p></div></div><form method="POST" action="{{ route('memberships.store',$organisation) }}" class="card form-grid">@csrf<label>{{ __('messages.email') }}<input type="email" name="email" required autocomplete="off"></label><p class="help">{{ __('messages.membership_email_help') }}</p><fieldset><legend>{{ __('messages.roles') }}</legend>@foreach($roles as $role)<label class="check"><input type="checkbox" name="roles[]" value="{{ $role->id }}"> {{ $role->display_name }}</label>@endforeach</fieldset><button class="btn primary">{{ __('messages.add_member') }}</button></form><div class="table-wrap mobile-card-table mt-6"><table><thead><tr><th>{{ __('messages.name') }}</th><th>{{ __('messages.email') }}</th><th>{{ __('messages.roles') }}</th></tr></thead><tbody>@foreach($memberships as $membership)<tr><td data-label="{{ __('messages.name') }}">{{ $membership->user->name }}</td><td data-label="{{ __('messages.email') }}">{{ $membership->user->email }}</td><td data-label="{{ __('messages.roles') }}">{{ $membership->roles->pluck('display_name')->join(', ') }}</td></tr>@endforeach</tbody></table></div>@endsection
