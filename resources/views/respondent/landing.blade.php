@extends('layouts.app')
@section('content')<div class="auth-card"><h1>{{ $publication->form->name }}</h1><p>{{ $publication->name }}</p>@if(!$publication->isOpen())<div class="notice error">{{ __('messages.publication_closed') }}</div>@else<form method="POST" action="{{ route('publications.start',$publication) }}" class="stack">@csrf @if($publication->access_mode==='access_code')<label>{{ __('messages.access_code') }}<input type="password" name="access_code" required></label>@endif @if(request('invite'))<input type="hidden" name="invite" value="{{ request('invite') }}">@endif<button class="btn primary">{{ __('messages.start') }}</button></form>@endif</div>@endsection
