@extends('layouts.app')
@section('content')<h1>{{ $organisation->exists?__('messages.edit'):__('messages.new_organisation') }}</h1><form class="card stack" method="POST" action="{{ $organisation->exists?route('organisations.update',$organisation):route('organisations.store') }}">@csrf @if($organisation->exists)@method('PUT')@endif<label>{{ __('messages.name') }}<input name="name" value="{{ old('name',$organisation->name) }}" required></label>@unless($organisation->exists)<label>Slug<input name="slug" value="{{ old('slug') }}"></label>@endunless @if($organisation->exists)<label class="check"><input type="checkbox" name="is_active" value="1" @checked($organisation->is_active)> {{ __('messages.active') }}</label>@endif<button class="btn primary">{{ __('messages.save') }}</button></form>@endsection
