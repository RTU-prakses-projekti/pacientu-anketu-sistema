@extends('layouts.app')
@section('content')<h1>{{ __('messages.new_questionnaire') }}</h1><form method="POST" action="{{ route('forms.store') }}" class="card stack">@csrf<input type="hidden" name="organisation_id" value="{{ $organisation->id }}"><label>{{ __('messages.questionnaire_name') }}<input name="name" required autofocus></label><fieldset><legend>{{ __('messages.preset') }}</legend>@foreach(['blank','patient_questionnaire'] as $preset)<label class="choice"><input type="radio" name="preset" value="{{ $preset }}" @checked($preset==='blank')> {{ __('messages.'.$preset) }}</label>@endforeach</fieldset><button class="btn primary">{{ __('messages.create') }}</button></form>@endsection
